DATABASE_URL=data/vereinsknete.db
# Set to "production" to serve static files, "development" to skip serving static files
ENVIRONMENT=production
# Production logging level
RUST_LOG=info
# Port for production (can be overridden by Docker)
PORT=8080
# Host binding for production
HOST=0.0.0.0
