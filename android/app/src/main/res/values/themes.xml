<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme -->
    <style name="Theme.YogaKnete" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Primary brand color -->
        <item name="android:colorPrimary">@color/yoga_purple</item>
        <item name="android:colorPrimaryDark">@color/yoga_purple_dark</item>
        <item name="android:colorAccent">@color/yoga_gold</item>
        
        <!-- Window background -->
        <item name="android:windowBackground">@color/yoga_background</item>
        
        <!-- Status bar -->
        <item name="android:statusBarColor">@color/yoga_purple_dark</item>
        <item name="android:windowLightStatusBar">false</item>
        
        <!-- Navigation bar -->
        <item name="android:navigationBarColor">@color/yoga_purple_dark</item>
        <item name="android:windowLightNavigationBar">false</item>
    </style>
    
    <!-- Splash Screen Theme -->
    <style name="Theme.YogaKnete.Splash" parent="Theme.YogaKnete">
        <item name="android:windowBackground">@drawable/splash_screen</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowIsTranslucent">false</item>
    </style>
</resources>
