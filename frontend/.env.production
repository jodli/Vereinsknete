# Production environment configuration for Home Assistant add-on
# Disable source map generation for production builds to reduce bundle size
GENERATE_SOURCEMAP=false
# Use relative API URL for Home Assistant ingress proxy compatibility
REACT_APP_API_URL=/api
# Disable development tools
REACT_APP_ENV=production
# Optimize build for add-on environment
INLINE_RUNTIME_CHUNK=false
# Reduce bundle size by disabling source maps and optimizing chunks
BUILD_PATH=build
