# Default environment configuration
# Disable source map generation to avoid warnings about missing third-party source maps
# This is the recommended setting for development to avoid noise from missing source maps
GENERATE_SOURCEMAP=false

# Alternative configuration (uncomment the line below and comment the line above if you need source maps):
# GENERATE_SOURCEMAP=true
